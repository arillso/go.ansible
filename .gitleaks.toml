# Gitleaks configuration for go.ansible
# https://github.com/gitleaks/gitleaks

title = "gitleaks config for go.ansible"

# Allowlist for test files containing PEM marker examples
[allowlist]
description = "Ignore test fixtures with fake SSH keys"
paths = [
    '''ansiblePlaybook_test\.go''',
]
regexes = [
    '''TEST-KEY-DATA''',
]
